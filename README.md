# Ask Nasa solo project

## Getting started:

After cloning the repository, you have to type `node backend/server.js` in a terminal to run the server. The site will be available at *http://127.0.0.1:9000*. 

